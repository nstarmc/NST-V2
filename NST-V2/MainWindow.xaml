<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NST_V2"
        xmlns:pages="clr-namespace:NST_V2"
        mc:Ignorable="d" Style="{StaticResource UiWindow}"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="NSTARMC Tools" Height="600" Width="1200" WindowStyle="None">
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0" MappingMode="RelativeToBoundingBox" SpreadMethod="Pad" Opacity="0.505">
            <GradientStop Color="#FF745BD0" Offset="1"/>
            <GradientStop Color="#FF41E4A4" Offset="0"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <ui:TitleBar Title="NSTARMC Tools" Grid.Row="0"/>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:NavigationCompact x:Name="RootNavigation" IsExpanded="True" Grid.Column="0"  Frame="{Binding ElementName=RootFrame}">
                    <ui:NavigationCompact.Items>
                        <ui:NavigationItem Content="首页" Icon="Home24" PageTag="dashboard"  PageType="{x:Type local:Pages_Home}"/>
                        <ui:NavigationItem Content="Java下载" Icon="Home24" PageTag="dashboard"  PageType="{x:Type local:Pages_Home}"/>
                        <ui:NavigationItem Content="版本管理" Icon="Home24" PageTag="dashboard"  PageType="{x:Type local:Pages_Home}"/>
                        <ui:NavigationItem Content="下载/更新整合包" Icon="Home24" PageTag="dashboard"  PageType="{x:Type local:Pages_Home}"/>
                        <ui:NavigationItem Content="帮助" Icon="Home24" PageTag="dashboard"  PageType="{x:Type local:Pages_Home}"/>
                    </ui:NavigationCompact.Items>
                    <ui:NavigationCompact.Footer>
                        <ui:NavigationItem  Content="设置"   Icon="Diversity24" />
                        <ui:NavigationItem Content="关于" Icon="Info12" PageType="{x:Type local:Pages_About}"/>
                    </ui:NavigationCompact.Footer>
                </ui:NavigationCompact>
                <ScrollViewer Grid.Column="1">
                    <Frame x:Name="RootFrame" />
                </ScrollViewer>
            </Grid>

        </Grid>
        <TextBlock Text="Alpha早期测试版本" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,10" Opacity="0.6"/>

    </Grid>
</Window>
